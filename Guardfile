# A sample Guardfile
# More info at https://github.com/guard/guard#readme

guard 'livereload' do
  guard :livereload do
    watch(%r{.+\.(css|js|html?|cfm|cfml|cfc|xml|inc|theme)$})
  end
  # # Sprockets
  # watch(%r{(app|vendor)(/assets/\w+/(.+\.(css|js|html))).*}) { |m| "/assets/#{m[3]}" }
end
